#pragma once

/// <summary>
/// Uses position based around window, does not account for any camera movement.
/// Used for UI and static images
/// </summary>
struct ScreenPosition
{
	float x = 0;
	float y = 0;
};